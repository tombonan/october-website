---

---

<html lang="en">
  <head>
    <meta charset="utf-8" />
    <link rel="icon" type="image/x-icon" href="/favicon.ico" />
    <meta name="viewport" content="width=device-width" />
    <title>Tom & Vera Wedding</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Spectral:ital,wght@0,200;0,300;0,400;0,500;1,200;1,300;1,400&display=swap" rel="stylesheet">
    <style>
      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html {
        font-family: 'Spectral', Georgia, serif;
        color: #2c2c2c;
        scroll-behavior: smooth;
      }

      .hero {
        position: relative;
        width: 100%;
        height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
      }

      .hero-background {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background-image: url('/stanta-barbara-mission.jpg');
        background-size: cover;
        background-position: center;
        filter: blur(4px);
        transform: scale(1.05);
      }

      .hero-overlay {
        position: absolute;
        top: 0;
        left: 0;
        width: 100%;
        height: 100%;
        background: rgba(0, 0, 0, 0.15);
      }

      .hero-content {
        position: relative;
        z-index: 1;
        text-align: center;
        color: #fff;
        text-shadow: 0 2px 8px rgba(0, 0, 0, 0.4);
        padding: 2rem;
      }

      .hero-content h1 {
        font-size: 5rem;
        font-weight: 400;
        font-style: italic;
        letter-spacing: 0.02em;
        margin-bottom: 0.2rem;
      }

      .hero-content h1 .amp {
        font-size: 2.6rem;
        vertical-align: middle;
      }

      .hero-content p {
        font-size: 1.25rem;
        font-weight: 200;
        letter-spacing: 0.1em;
      }

      .hero-content p.location {
        font-weight: 400;
        font-style: italic;
      }

      .split-section {
        display: flex;
        width: 100%;
        min-height: 50vh;
        position: relative;
      }

      .split-section::after {
        content: '';
        position: absolute;
        left: 50%;
        top: 15%;
        height: 70%;
        width: 1px;
        background-color: #362420;
      }

      .split-left,
      .split-right {
        flex: 1;
        background-color: #fdf7ea;
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        padding: 4rem 2rem;
        text-align: center;
        color: #362420;
      }

      .split-left h2,
      .split-right h2 {
        font-size: 4rem;
        font-weight: 200;
        margin-bottom: 1rem;
        font-style: italic;
      }

      .split-left .divider,
      .split-right .divider {
        width: 18px;
        height: 1px;
        background-color: #2c2c2c;
        margin-bottom: 1rem;
      }

      .split-left p,
      .split-right p {
        font-size: 1.1rem;
        font-weight: 200;
        font-style: italic;
        line-height: 1.8;
      }

      .split-section p.burgundy-highlight a {
        color: #6c001b;
        font-weight: 500;
        font-size: 1.2rem;
        font-style: normal;
        text-decoration: none;
        transition: color 0.6s;
      }

      .split-section p.burgundy-highlight a:hover {
        color: #9a1035;
      }

      .ordinal {
        vertical-align: super;
        font-size: 0.65rem;
      }

      .registry-section {
        background-color: #6c001b;
        color: #fdf7ea;
        padding: 4rem 2rem;
        text-align: center;
        display: flex;
        flex-direction: column;
        align-items: center;
      }

      .registry-section h2 {
        font-size: 2.5rem;
        font-weight: 200;
        font-style: italic;
        margin-bottom: 1rem;
      }

      .registry-section .divider {
        width: 18px;
        height: 1px;
        background-color: #fdf7ea;
        margin-bottom: 1rem;
      }

      .registry-section p {
        font-size: 1.1rem;
        font-weight: 300;
        margin-bottom: 1.5rem;
        max-width: 400px;
      }

      .registry-section a {
        color: #fdf7ea;
        font-size: 1rem;
        font-weight: 400;
        text-decoration: none;
        border: 1px solid #fdf7ea;
        padding: 0.75rem 2rem;
        transition: background-color 0.4s, color 0.4s;
      }

      .registry-section a:hover {
        background-color: #fdf7ea;
        color: #6c001b;
      }

      @media (max-width: 480px) {
        .hero-content h1 {
          font-size: 2.5rem;
        }

        .hero-content p {
          font-size: 1rem;
        }

        .split-section {
          flex-direction: column;
        }

        .split-section::after {
          left: 15%;
          top: 50%;
          width: 70%;
          height: 1px;
        }

        .split-left,
        .split-right {
          min-height: 30vh;
          padding: 6rem 2rem;
        }
      }
    </style>
  </head>
  <body>
    <section class="hero">
      <div class="hero-background"></div>
      <div class="hero-overlay"></div>
      <div class="hero-content">
        <p>October 2<span class="ordinal">nd</span>, 2026</p>
        <h1>Tom <span class="amp">&</span> Vera</h1>
        <p class="location">Santa Barbara, California</p>
      </div>
    </section>

    <section class="split-section">
      <div class="split-left">
        <h2>Ceremony</h2>
        <div class="divider"></div>
        <p class="burgundy-highlight"><a href="https://maps.app.goo.gl/nKLLJAet7cQpXarQA" target="_blank" rel="noopener noreferrer">Santa Barbara Courthouse</a></p>
        <p>3pm in the Mural Room</p>
      </div>
      <div class="split-right">
        <h2>Reception</h2>
        <div class="divider"></div>
        <p class="burgundy-highlight"><a href="https://maps.app.goo.gl/C1tHnYVGthRtC3af7" target="_blank" rel="noopener noreferrer">La Paloma Cafe</a></p>
        <p>5:30pm on the Patio</p>
      </div>
    </section>

    <section class="registry-section">
      <h2>Gifts</h2>
      <div class="divider"></div>
      <p>Please contribute to Tom's white wine fund!</p>
      <a href="https://www.zola.com/registry/tomandvera2026" target="_blank" rel="noopener noreferrer">View Registry</a>
    </section>
  </body>
</html>
